class Group {
  var userList = [];
  var userIDList = [];
  var userNameList = [];
  var groupIDList = [];
  var groupNameList = [];
  var groupName;
  var username;
  var selection;

  Group(this.userList, this.userIDList, this.userNameList, this.groupIDList,
      this.groupNameList, this.groupName, this.username, this.selection);
}
